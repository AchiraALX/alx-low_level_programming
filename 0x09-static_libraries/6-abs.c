#include "main.h"

/**
 * _abs - Returns the absolute value of a number
 * @n: type int number
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
