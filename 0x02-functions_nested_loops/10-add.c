#include "main.h"

/**
 * add - Adds two numbers and returns the result.
 * @y: the firs number type int
 * @x: the second number type int
 * Return: Always 0 (Success)
 */

int add(int y, int x)
{
	return (x + y);
}
