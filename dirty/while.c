#include "head.h"

/**
 * main - main function
 * 
 * Return: Always 0 (Success)
*/

int main(void)
{
        int count;

        count = 0;

        while (count <= 5)
        {
                printf("%i\n", count);
                count++;
        }

        return (0);
}