#include "head.h"

/**
 * main - takes input from a .txt file and displays it in the terminal
 *
 * Return: Always (0) Success
 */

int main ()
{
	int c;

	while ((c = getchar()) != EOF)
	{
		putchar (c);
	}

	return (0);
}
